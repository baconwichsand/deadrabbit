import csv


# Read column in csv to array
# Optional parameter: dtype, parse to specific datatype
def read_csv_column(filename, column_id, dtype=float):
    output = []
    f = open(filename, 'rt')
    reader = csv.reader(f)
    for row in reader:
        output.append(dtype(row[1]))
    f.close()
    return output
